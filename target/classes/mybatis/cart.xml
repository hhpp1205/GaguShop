<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cart">
    <insert id="addCart">
        INSERT INTO cart
        VALUES (NULL, #{memberId}, #{gaguId})
    </insert>

    <select id="checkCart" resultType="Integer">
        SELECT COUNT(*) FROM cart
        WHERE member_id = #{memberId} AND gagu_id = #{gaguId}
    </select>

    <select id="cartList" resultType="Gagu">
        SELECT g.*,c.id cart_id FROM cart c, gagu g
        WHERE c.member_id = #{memberId} AND c.gagu_id = g.id;
    </select>

    <delete id="deleteCart">
        DELETE FROM cart
        WHERE id = #{id}
    </delete>
</mapper>